// src/data/items.js
// Turn the old JSON into a JS module.

const items = {
  // ⬇️ paste the big object you posted (warrior/princess/wizard/dragon/fairy/…)
  // exactly where this comment is, starting with: "warrior": { ... }, "princess": { ... }, ...
  // and wrap it inside this top-level object.
};

export default items;
